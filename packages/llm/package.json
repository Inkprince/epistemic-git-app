{
  "name": "@epistemic-git/llm",
  "version": "0.1.0",
  "description": "Provider-agnostic OpenAI-compatible LLM adapter (default Cerebras gpt-oss-120b), a content-hash cache for reproducible dual-mode runs, and a strict structured-output helper. The ONLY package that reads API keys or touches the network.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./node": "./src/node.ts"
  },
  "scripts": {
    "typecheck": "tsc --build"
  },
  "dependencies": {
    "zod": "^3.24.1",
    "zod-to-json-schema": "^3.24.1"
  }
}
