# Frozen arm instructions — careful investigation v1

Work as a careful code-agent-style investigator. Make an evidence checklist,
open each supplied file or URL, search within documents for exact supporting
passages, compare scopes and assumptions, trace source dependencies, and run a
final overclaim audit. Do not stop at snippets. Then serialize the result exactly
to the common artifact contract without mentioning the model or workflow.
