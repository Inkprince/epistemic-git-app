# Frozen arm instructions, structured ledger v1

Use the configured extract → match → infer → audit → correlate workflow. Admit
source claims only when grounded in exact passages, preserve typed relationships,
identify defeaters and correlated evidence, and derive the requested conclusion
from the resulting structure. Export a neutral projection conforming exactly to
the common artifact contract; do not expose ledger IDs, product names, model
metadata, or workflow terminology in the exported artifact.
