# Frozen arm instructions — deep research v1

Use the configured native deep-research workflow. Search and read the supplied
URLs, trace primary sources where the source packet permits it, and check whether
apparently separate citations share authors, data, or citation ancestry. Spend no
more than the configured tool-call budget. Then serialize the result exactly to
the common artifact contract without mentioning this workflow.
